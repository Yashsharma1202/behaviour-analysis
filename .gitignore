# Include everything (all raw per-stock data + downloaded NSE feeds + outputs),
# except build artifacts and the personal chat log.
__pycache__/
*.pyc

# personal chat history (also references the separate ML Regime project — keep out)
all_user_requests.md
